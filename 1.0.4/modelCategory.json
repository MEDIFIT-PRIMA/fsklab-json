{
    "id": "https://raw.githubusercontent.com/MEDIFIT-PRIMA/fsklab-json/schema_view/1.0.4/modelCategory.json",
    "type": "object",
    "properties": {
        "modelClassComment": {
            "type": "string",
            "description": "Empty"
        },
        "basicProcess": {
            "type": "array",
            "items": {
                "type": "string",
                "description": "Defines the impact of the process on the hazard (so far only valid for process models and predictive models)"
            }
        },
        "modelClass": {
            "type": "string",
            "description": "Type of model according to RAKIP classification"
        },
        "modelSubClass": {
            "type": "array",
            "items": {
                "type": "string",
                "description": "Sub-cassification of the model given the Model Class"
            }
        }
    },
    "required": []
}